apiVersion: 1

datasources:
  - name: Tempo
    uid: tempo
    type: tempo
    access: proxy
    url: http://tempo.dev.local:3200
    isDefault: true
    editable: true
    jsonData:
      httpMethod: GET
